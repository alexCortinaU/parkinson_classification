pl_trainer:
  max_epochs: 100
  devices:
  - 0
  accelerator: gpu
  precision: 16
  log_every_n_steps: 5

training:
  early_stopping:  False
  monitor_ckpt: val_auroc # val_acc, val_f1, val_auroc

dataset:
  train_num_workers: 4
  train_batch_size: 9
  val_num_workers: 4
  val_batch_size: 9
  reshape_size: 180
  patch_size: 64
  map_type: 
    - MTsat
    # - R1
  queue_length: 50
  samples_per_volume: 9
  random_state: 42
  windowed_dataset: True
  brain_masked: True
  shuffle: True

# should match arguments in the module class
model:
  chkpt_path: /home/alejandrocu/Documents/parkinson_classification/p1_ppmi_outs/3dresnet-da01_bz10_focal_sampler/version_0/checkpoints/epoch=01-val_f1=0.7708.ckpt 
  n_classes: 2
  in_channels: 4
  loss: focal # dice, cross_entropy, bin_cross_entropy, focal, tversky
  optimizer_class: adam # sgd, adam, rmsprop
  learning_rate: 0.001
  sch_patience: 50
  momentum: 0 # 0.9  
  weight_decay: 0 # 0.0001    

exp_name: brainhmri3dresnet-da02_bz3_focal_adam_lr0.001-v4
